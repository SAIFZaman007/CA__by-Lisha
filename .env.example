# Leave blank to use the dev proxy / same-origin deploy (recommended).
# Set only when the API lives on a different origin, e.g. https://api.autonomyfitness.press
VITE_API_URL=

VITE_SITE_URL=https://autonomyfitness.press
# Google Analytics 4 — leave blank until the property exists.
VITE_GA_MEASUREMENT_ID=